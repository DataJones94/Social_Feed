import React from 'react';
import ReactDOM from 'react-dom';
import App from './App.js';



 ReactDOM.render(<App />,document.getElementById('root'));


{/*render function takes in two arguements, the higher componendt-App component and we can use it like
defining what our root component is. take it compilet and combine it and inject it to the index.html file
that has 'root'*/}
